<% content_for :admin do %>
  <p> You are an Admin! </p>

<h1>Editing User</h1>

<%= render 'form', user: @user %>

<%= link_to 'Show', @user , class: 'button primary' %>
<%= link_to 'Back', users_path , class: 'button primary'  %>

<br>


<% end %>

<% content_for :lecturer do %>
  <P> You are an Lecturer! </P>
  <p> This is the User Edit Page for Lecturer! </p>
  <P> So there is nothing here. T_T & LOL </P>

  <h1>Editing Yourself's Info </h1>

<%= render 'formfornonadmin', user: current_user %>

<%= link_to 'Show', current_user, class: 'button primary'  %>

<%= link_to 'Back', users_path , class: 'button primary' %>
<% end %>

<% content_for :student do %>
  <p> You are an Student! </p>
  <p> This is the User Edit Page for Student! </p>
  <P> So there is nothing here. T_T & LOL </P>

<h1>Editing Yourself's Info </h1>

<%= render 'formfornonadmin', user: current_user %>


<%= link_to 'Show', current_user, class: 'button primary'  %>

<%= link_to 'Back-tobefix', users_path, class: 'button primary'  %>
<% end %>
